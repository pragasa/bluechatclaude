{
  "name": "bluechat-backend",
  "version": "1.0.0",
  "description": "BlueChat backend API server",
  "main": "src/server.js",
  "scripts": {
    "build": "node --check src/server.js && node --check src/models/index.js && node --check src/routes/auth.js && node --check src/routes/messages.js",
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "npm run build"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
