{
  "name": "bluechat-backend",
  "version": "1.0.0",
  "description": "BlueChat backend API server",
  "main": "server.js",
  "scripts": {
    "build": "node --check server.js && node --check index.js && node --check auth.js && node --check messages.js",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "npm run build"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
